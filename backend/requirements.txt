# PRISM backend dependencies.
# Kept in sync with the repo-root requirements.txt (Render installs from root).
# Core API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
python-dotenv>=1.0.1
agora-token-builder
pydantic>=2.7.1
websockets>=12.0
# LLM (pluggable provider adapter)
litellm>=1.40.0
# Server-side speech features (optional at runtime — /asr and /ws/vad):
#   Faster Whisper ASR + Silero VAD both require torch.
faster-whisper>=1.0.0
silero-vad>=5.1.2
torch>=2.0.0
numpy>=1.24.0
