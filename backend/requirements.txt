# PRISM backend dependencies.
# Kept in sync with the repo-root requirements.txt (Render installs from root).
# Core API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
python-dotenv>=1.0.1
agora-token-builder
pydantic>=2.7.1
websockets>=12.0
# LLM (pluggable provider adapter)
litellm>=1.40.0
# Server-side speech features (optional at runtime — /asr and /ws/vad):
#   Faster Whisper ASR + Silero VAD both require torch.
faster-whisper>=1.0.0
silero-vad>=5.1.2
torch>=2.0.0
numpy>=1.24.0
sqlmodel>=0.0.18
aiosqlite>=0.19.0
slowapi>=0.1.9
# ── Memory + Knowledge (Phase 2, optional at runtime) ───────────────
# Embeddings for semantic vector search.
# Without these the knowledge base falls back to keyword search.
sentence-transformers>=2.2.0
# python-jose for JWT auth (already required by Phase 1 auth, listed explicitly)
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
